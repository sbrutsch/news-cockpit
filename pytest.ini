[pytest]
# Ohne diese Zeile findet `pytest -q` das Paket `app` nicht. Bei
# `python -m pytest` faellt das nicht auf, weil das Modul-Flag das
# Arbeitsverzeichnis selbst in den sys.path legt — die CI ruft aber `pytest`.
pythonpath = .
testpaths = tests
